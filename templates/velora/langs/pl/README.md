# noctra

Dark exchange-terminal template for Offerra.

- **Langs (v1):** English root (`SITE_LANG=en`) + `langs/{fr,es,it,fi}`
- **Look:** dark desk + amber CTA + emerald live accents
- **Logic:** same `integration/` stack as `default` (form token, LeadProcessor, Keitaro, mirrors/CWV)
- **SEO:** hardened apex `canonical_url()`, 404 with `noindex`, rephrased EN copy + different section layout vs `default`
